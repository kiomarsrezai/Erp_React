import { generalFieldsConfig } from "../general-fields-config";

export const programConfig = {
  area: generalFieldsConfig.AREA,
  kind: "programId",
};

export const programURLS = {
  list: "ProgramApi/ProgramOperation",
  combo: "ProgramApi/ProgramList",
};
